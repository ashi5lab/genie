import Headernav from "../nav/nav";

function Home() {

    return ( <div>
        <header className="App-header">
        <Headernav></Headernav>
      </header>

    </div> );
}

export default Home;